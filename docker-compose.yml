version: "3.9"

services:
  fakebank-mock:
    build:
      context: ./qa-digital-bank-lab
      dockerfile: Dockerfile
    container_name: fakebank-mock
    ports:
      - "3000:3000"
    restart: unless-stopped
